#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main() {
	// Most significant digit is tied to least significant digit.
	// Therefore, we get the least significant digit starting from before (for odd) or equal to middle digit (for even)
	// and increase it by one?
}